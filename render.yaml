services:
  - type: web
    name: socionet-api
    runtime: node
    rootDir: apps/api
    buildCommand: npm install
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production